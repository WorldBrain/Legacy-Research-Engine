import React from 'react';
import './searchView.scss';

export const SearchView = () => {
    return (
        <div>
            Hello from Search!
        </div>
    );
};

export default SearchView;